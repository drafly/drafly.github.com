# Archive

这里保存网站重构前的历史资料，不参与当前 Astro 网站的安装、构建或部署。

- `jekyll-site/`：原 Jekyll 配置、模板、页面、脚本、样式、字体和图片。
- `research-sources/`：用于内容整理的本地论文资料；该目录继续受仓库根目录 `.gitignore` 规则约束，不提交论文 PDF。
- `CODEX_HANDOFF.md`：重构阶段的历史交接记录，仅供追溯。

当前网站只依赖仓库根目录的 Astro 配置，以及 `src/`、`public/` 和 `.github/workflows/`。
